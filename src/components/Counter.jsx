const Counter = () => {
return(
  <div>
  <h1>Counter</h1>
  <button onClick={increise}>+1</button>
  <button onClick={reduce}>+1</button>
  <button onClick={reset}>reset</button>
  </div>
)
}

export default Counter;